<h1>Public prediction page
<p>On #{show $ predictionCreated predict}, #{predictionName predict} made a prediction. Following is the SHA512 hash of this prediction for verification:

<pre>#{sha512}

<p>You will need to wait until #{predictionName predict} makes this prediction public to see its content.

<h6>New to the site? Learn more on the <a href=@{HomeR}>homepage</a>.
